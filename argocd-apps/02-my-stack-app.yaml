apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: my-monitoring-stack
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  source:
    repoURL: https://github.com/LitvinchukRoman/FormaTest.git
    targetRevision: HEAD
    path: helm-charts/my-monitoring-stack
    helm:
      valueFiles:
        - values.yaml
      parameters:
        - name: "helm.dependency-update"
          value: "true"
  destination:
    server: https://kubernetes.default.svc
    namespace: my-app

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
